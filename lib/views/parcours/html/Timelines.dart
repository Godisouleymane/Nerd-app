import 'package:flutter/material.dart';

class TimeLinesWidget extends StatelessWidget {
  const TimeLinesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}